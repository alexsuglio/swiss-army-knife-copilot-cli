#!/usr/bin/env bash
set -euo pipefail

ROOT_DIR="$(git rev-parse --show-toplevel 2>/dev/null || pwd)"
OVERRIDE_FILE="$ROOT_DIR/.demo-allow-commits"
ALLOW_CONFIG="$(git -C "$ROOT_DIR" config --bool --get demo.allowCommits 2>/dev/null || echo false)"

if [ "${ALLOW_DEMO_COMMIT:-}" = "1" ] || [ -f "$OVERRIDE_FILE" ] || [ "$ALLOW_CONFIG" = "true" ]; then
  exit 0
fi

cat <<'MSG'
[demo-guard] Commit blocked to protect the demo sandbox.

To intentionally commit, prefix your command with ALLOW_DEMO_COMMIT=1:
  ALLOW_DEMO_COMMIT=1 git commit -m "..."
MSG

exit 1
