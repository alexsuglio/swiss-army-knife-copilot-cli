{
  "name": "copilot-cli-demo-sample-app",
  "version": "1.0.0",
  "private": true,
  "description": "Sample app for Copilot CLI demo scenarios",
  "type": "module",
  "scripts": {
    "test": "node --test tests/**/*.test.js",
    "test:watch": "node --test --watch tests/**/*.test.js"
  },
  "engines": {
    "node": ">=20"
  }
}
